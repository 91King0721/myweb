# 整学期空闲教室查询系统

一个查看全校教室空闲情况的交互式课表查询工具，支持收藏、折叠、自定义时间段筛选，主要内容由CC编写。

> **当前版本:** v0.5.1 — [在线体验](https://655512.xyz/)

---

## 版本历史

### v0.5.1（当前版本）

- 暂时隐藏首页“单词测试”导航和启动按钮，词汇功能与本地数据保持不变

### v0.5.0

- 新增按天执行的艾宾浩斯今日复习，可自由设置本次复习数量
- 新增单词熟练度和遗忘率
- 新增每日测试统计、最近 7 天曲线和 61 个 List 掌握率
- 新增高频错词专项与按错误次数智能抽题
- 学习记录采用紧凑本地存储，并保留原有错题、收藏和测试进度

### v0.4.3

- 新增全部错题乱序测试
- 支持多选有错题的 List 后混合乱序测试
- 乱序进度轻量恢复，不清空现有本地数据

### v0.4.2

- 错题专项测试支持按实际含有错题的 List 单独复习
- 可在“全部错题”和各 List 错题之间自由切换

### v0.4.1

- 新增错题 JSON 导出与合并导入，方便跨设备迁移
- 新增低占用测试进度恢复，网页更新时保留原有本地数据
- 错题按做错次数排序，并划分高频、中频、低频错题
- 错题本只显示实际含有错题的 List 筛选项

### v0.4

新增“考研高频词测试终端”，同时保留原有空闲教室查询功能：

- 收录 List 1-61 共 1826 个词条，支持四选一、键入和语音朗读
- 新增独立错题本与单词收藏，支持搜索、筛选和专项复习
- 学习数据保存在本设备，并适配电脑、iPad 和手机
- 延续原站工业 UI 风格

### v0.3
在保留现代工业终端风格的基础上，重点优化响应速度、移动端体验和细节质感：

#### ⚡ 响应速度优化

- 新增桌面端与移动端 WebP 背景图，保留原 PNG 作为兼容兜底
- 首页提前预取查询页脚本与文档，降低首次点击“启动查询终端”的等待感
- 查询页预加载关键脚本和背景资源，减少进入时的资源竞争
- 导航跳转前预热目标页资源，避免过场动画与资源加载叠加造成卡顿
- 查询结果改为只渲染当前展开日期的表格，折叠日期不再生成隐藏大表
- 刷新动效移除强制同步布局，改用 `requestAnimationFrame` 调度

#### 📱 移动端优化

- 移动端关闭重型页面扫光过场、背景漂移动画和导航模糊，保证点击响应更轻
- 查询控件改为触控友好的紧凑网格布局，极窄屏自动切换为单列
- 移动端进入查询页后自动定位到当前展开日期，减少手动查找
- 保持表格横向滚动，避免页面整体横向溢出

#### ✨ 细节高级感

- 新增轻量 CSS 精修层，提升面板、控件、表格和标题区域的层次感
- 增强纸面高光、工业细线、锐利阴影和焦点状态
- 桌面端增加克制的 hover 微反馈，移动端继续保持低动效
- 新增 `scripts/perf-check.mjs`，锁定性能和视觉优化的关键约束

### v0.2
完整 UI 重设计，全面更换为全新的工业风格：

#### 🎨 UI 重设计

- 全新工业风格主题（深色系 + 高对比点缀色）
- 深色主题配色：黑底 + 亮黄点缀 + 蓝/红辅助色
- 工业代码风格字体体系（JetBrains Mono + Inter）
- 斜角裁切面板、L 型角标装饰、网格点阵背景
- 导航栏增加系统标签 `// SYS_NAV`
- 首页背景图配蓝黄双色渐变光晕
- 工业风格 CTA 按钮（斜角裁切 + 光晕 hover 效果）
- 全新表格菱形占用/空闲指示器
- 全局 WebKit 滚动条样式
- 响应式保留并适配新风格

#### 🖼 视觉效果

- 首页背景支持桌面/手机双图（background.jpg / phonebg.jpg）
- 标题旁增添 `// 01` 装饰序号
- 入场动画（fadeInUp）渐次播放
- 鼠标悬停 L 角标高亮为黄色
- 表格行 hover 增强高亮
- 小尺寸设备移除 clip-path 以保证 sticky 列兼容

### v0.1
新增三大功能，全面提升查询效率：

#### ✨ 收藏功能

- 点击教室旁的星标即可收藏，数据保存在浏览器本地
- 控制栏新增"收藏"切换按钮，一键筛选仅看已收藏教室
- 已收藏教室行高亮显示，易于识别

#### 📅 折叠式日程

- 一周五天改为手风琴式折叠，默认展开当天
- 点击任意天标题展开/收起，每次仅展开一天，界面更清爽
- 进入页面自动定位到当前周次和当天

#### ⏱ 时间段筛选

- 预设三个时段：上午(1-4节)、下午(5-8节)、晚上(9-12节)
- 新增**自定义时段**：自由选择起始和结束节次（如 1-9、5-12、3-6）
- 表格仅显示该时段列，空闲计数实时更新

> 完整变更清单见下方 [v0.0 → v0.1 变更说明](#v00--v01-变更说明)

### v0.0（初始版本）
首次发布的基础功能：

- 欢迎页（一级菜单入口）
- 按周次（1-18周）和教学楼筛选教室
- 教室名称文本搜索
- 每日空闲教室统计
- Apple 风格界面设计
- 响应式布局

---

## 功能

- **欢迎页**：一级菜单入口，提供"查询空闲教室"入口
- **教室查询**：按周次和教学楼筛选，支持教室名称搜索
- **单词测试**：List 1-61 自由切换，支持四选一、键入和朗读
- **单词错题本**：独立页面查看所有错词，支持频率分级、次数排序、动态 List 筛选、导入导出、专项测试和多 List 乱序测试
- **单词收藏**：独立页面查看收藏词汇，支持搜索、List 筛选、朗读、移除和专项测试
- **学习进度**：艾宾浩斯今日复习、熟练度、遗忘率、每日统计、7 天曲线和 List 掌握率
- **智能训练**：高频错词专项与按错误次数加权抽题
- **时间段筛选**：预设上午/下午/晚上 + 自定义任意节次范围
- **教室收藏**：星标收藏常用教室，一键过滤查看
- **折叠日程**：手风琴式展开收起，默认展开当天
- **空闲统计**：每节课占用/空闲情况一目了然
- **响应式设计**：完美适配桌面端、平板和手机

## 项目结构

```
├── index.html              # 欢迎页
├── query.html              # 查询页（核心功能）
├── vocabulary.html         # 考研高频词测试页
├── wrong-book.html         # 独立单词错题本
├── favorites.html          # 独立单词收藏
├── learning.html           # 学习进度与艾宾浩斯复习中心
├── CNAME                   # 自定义域名绑定
├── README.md
├── .gitignore
├── scripts/
│   ├── perf-check.mjs
│   ├── vocabulary-check.mjs
│   └── build-vocabulary-data.mjs
└── assets/
    ├── css/
    │   └── style.css       # 全局样式（工业风格）
    ├── images/
    │   ├── background.jpg  # 首页背景图（桌面端）
    │   └── phonebg.jpg     # 首页背景图（手机端）
    └── js/
        ├── data.js         # 课程数据（18周 × 4教学楼）
        ├── app.js          # 查询逻辑（渲染、收藏、筛选、折叠）
        ├── vocabulary-data.js
        ├── vocabulary-app.js
        ├── vocabulary-library.js
        ├── vocabulary-learning-core.js
        └── vocabulary-learning-dashboard.js
```

## 本地使用

直接在浏览器打开 `index.html` 即可，无需任何构建工具或依赖。

## 部署

部署到 GitHub Pages：

```bash
git push origin main --tags
```

然后在 GitHub 仓库的 Settings > Pages 中选择 `main` 分支，根目录 `/` 即可。

## 技术栈

- 纯原生 JavaScript（零依赖）
- CSS 自定义属性（深色模式、响应式）
- localStorage 持久化教室收藏、单词收藏、错题、测试偏好、学习统计和轻量测试进度
- Web Speech API 单词朗读
- 工业风格 UI（深色主题 + 等宽/无衬线字体体系）

---

## v0.0 → v0.1 变更说明

### 新增文件
- 无新增文件，全部在现有文件上修改

### 修改文件

| 文件 | 改动 |
|------|------|
| `query.html` | 添加时间段下拉框（含自定义选项）、收藏切换按钮、自定义范围选择器 |
| `assets/js/app.js` | 新增收藏管理（localStorage）、时间段过滤、折叠展开逻辑、自动检测当前周/天 |
| `assets/css/style.css` | 新增收藏星标/行高亮/按钮样式、折叠动画、自定义范围选择器样式、深色模式适配 |
| `assets/js/data.js` | 修正课程时间段映射（改为双节次配对时间） |

### 详细功能列表

**收藏系统**
- `localStorage` 存储，key 格式 `"教学楼|教室名"` 确保跨楼同名教室可区分
- 星标 SVG 点击切换，事件委托处理（兼容 SVG 子元素点击）
- 收藏过滤开关，激活时仅显示已收藏教室

**折叠式日程**
- 单例展开模式（同时仅展开一天）
- 点击折叠图标或整行均可切换
- 页面加载自动展开当天

**时间段筛选**
- 三个预设时段组：1-4节（08:00-12:00）、5-8节（14:00-18:00）、9-12节（19:00-22:30）
- 自定义时段：选择起始和结束节次，支持任意范围（自动处理起止颠倒）
- 过滤器流水线：文本搜索 → 收藏过滤 → 时段过滤

**时间段修正**
- 将原独立45分钟小节时间改为双节次配对时间
- 每对节次显示起止时间（如 1节=08:00、2节=09:50 对应 1-2节 08:00-09:50）

### 技术细节
- `getCurrentWeek()` 从 2026年3月9日（第1周周一）推算当前周次，自动适配学期范围 1-18周
- `getCurrentDayName()` 自动判断当天星期，周末默认展开周一
- 选中收藏行通过 `tr.fav-row` 高亮（#fffbeb / 深色模式 #2a2510）
